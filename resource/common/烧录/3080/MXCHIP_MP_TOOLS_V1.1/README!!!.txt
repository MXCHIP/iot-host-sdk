注意：
1. 必须以“管理员身份”运行此程序。
2. 若无法正常运行，请打开“Windows 7”兼容模式。
3.若使用factory模式，烧录固件必须是full bin，此时已经锁定界面，每次开启默认到factory mode，如果打算更换到Normal Mode ,请执行下MXCHIP_IMG_TOOL_RESET程序。即可恢复。
4.使用ATE固件，则需要先将COM口更新到3串口。方可读取模块。